# Journal — 2026-08-19 — Quality Gate Restoration Closure

## What happened

The falsification campaign closed with a NO-GO: all 24 hypothesis classes
tested across TA, funding rates, ML regime detection, cross-asset pairs,
sentiment, composites, and market-structure signals produced **zero
persistent out-of-sample positive expectancy** on 2026 data. The last
candidate (funding × price extreme interaction) scored 10/162 OOS passes
(6%), aggregate PnL -$455,090, regime-locked to the mid-2022 bear market —
pure overfitting.

The system is honest: it reports losses, refuses to trade live capital, and
never fabricates a positive result. That is the correct scientific outcome.

## What I did today

- **Fixed doc commit-hash misattribution.** Two code-reviewer agents found
  that the quality-gate restoration work was attributed to `e113f39`, but
  `f0b0ce7` is the commit that actually created `archive/falsification/`,
  removed the 5 stale knip `ignoreFiles` entries, and deleted the dead
  `evaluator/data-fetcher.ts` stub. `e113f39` only deleted the `src/` copies
  afterward. Corrected in both `docs/development-roadmap.md` and
  `docs/project-changelog.md`.
- **Fixed test-count stat drift.** Roadmap said "1880 across 119 files"; the
  repo has 130 `.test.ts` files. Updated to 130.
- **Fixed OI timeframe mismatch.** `scripts/alpha-real-data-backtest.ts`
  hardcoded `'1h'` for the open-interest fetch regardless of the CLI
  `timeframe` argument. `fetchOpenInterestHistory` accepts a literal union of
  periods, so I added a `VALID_PERIODS` set with a fallback to `'1h'` and a
  warning log for unsupported values — the script still never aborts a
  research run on an invalid period.
- **Verified the gate.** `npm run quality:gate` exits 0: 1880/1880 tests,
  0 lint warnings, 0 TS errors, knip clean.

## Why it matters

The falsification result is only trustworthy if the infrastructure that
produced it is itself trustworthy. A doc that misattributes work to the wrong
commit erodes that trust — anyone tracing provenance would draw the wrong
conclusion about what actually happened. The fix is small but it keeps the
record accurate.

## Closed

`/ak:cook --auto --parallel` was invoked with the accepted brainstorm
contract "next task go live" — deploy the CashClaw paper-trading platform to
Cloudflare Workers production. That task is now complete (see
`journal-2026-08-19-deploy.md`). The ak-cook brainstorm gate is satisfied for
that task; a new task would need its own contract.

## Open item

`ENCRYPTION_KEY` secret is still unset on Cloudflare. Set it via
`wrangler secret put ENCRYPTION_KEY` before any customer stores exchange
credentials — otherwise those credentials persist in plaintext. Not a
data-loss risk today: `api_credentials` is empty in production D1 and
`getEncryptionKey()` falls back to passthrough when the key is absent.

The NO-GO was specific to OHLCV/funding/OI signals. New directions with
different inputs (on-chain data, order book, social sentiment) remain
unexplored and would be a legitimate next task.